export const DEFAULT_OFFERS_COUNT = 7;
export const DEFAULT_OFFERS_PAGE = 1;
export const IMAGE_MAX_LENGTH = 256;

export enum OfferTitleLength {
  Min = 10,
  Max = 100,
}

export enum DescriptionLength {
  Min = 20,
  Max = 1024,
}

export enum ArtikulLength {
  Min = 5,
  Max = 40,
}

export enum PriceValue {
  Min = 100,
  Max = 1000000,
}
